overwrite: true
schema:
  - https://graphql.contentful.com/content/v1/spaces/${CONTENTFUL_SPACE_ID}/environments/master?access_token=${CONTENTFUL_ACCESS_TOKEN}
generates:
  ./generated/src/index.ts:
    plugins:
      - typescript
      - typescript-operations
